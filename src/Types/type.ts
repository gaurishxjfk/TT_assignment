export interface MenuItems {
  id: number;
  title: string;
  link: string;
}

export interface ProductSliderItems {
  id: number;
  title: string;
  description: string;
  isCurrentIndex?: boolean
}
